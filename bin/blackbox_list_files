#!/usr/bin/env bash

#
# blackbox_list_files.sh -- List files that black box is tracking
#
if [ -z "$BLACKBOX_HOME"]; then
  BLACKBOX_HOME=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
fi

set -e 
blackbox_home=$BLACKBOX_HOME
source "${blackbox_home}/_blackbox_common.sh"
cat "$BB_FILES"
